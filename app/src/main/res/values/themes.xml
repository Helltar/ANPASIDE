<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- only the window shell shown before compose draws, the ui itself is themed in ui/theme -->
    <style name="Theme.Anpaside" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
